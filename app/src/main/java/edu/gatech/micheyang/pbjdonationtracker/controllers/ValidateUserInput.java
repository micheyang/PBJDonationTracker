package edu.gatech.micheyang.pbjdonationtracker.controllers;

import android.content.Context;


public class ValidateUserInput {

    private Context context;

    public ValidateUserInput(Context context) {
        this.context = context;
    }

    /*
    this class will be used to validate or check user inputs.
    we can implement later on for extra credit.
    ideas to implement:
        -   Check if user inputs a null argument.
        -   Check if email address is valid (e.g. '@', '.com', etc).
        -   Check if password meets requirements (enhance security).
        -   Check if username contains invalid characters (e.g. '$', '*', etc).
        -   Check if 2 inputs match (security or things like confirm email).
     */
}
